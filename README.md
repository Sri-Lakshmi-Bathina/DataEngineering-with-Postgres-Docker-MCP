# DataEngineering-with-Postgres-Docker-MCP
In this project, I created and optimized a PostgreSQL database in a Docker container using MCPs within a Cursor. PostgreSQL is a relational database management system (RDBMS). Docker is used to create and manage the Postgres environment.
